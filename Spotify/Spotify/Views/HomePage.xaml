<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:templates="clr-namespace:Spotify.Templates"
             x:Class="Spotify.Views.HomePage"
             NavigationPage.HasNavigationBar="False"
             Title="Rock">
    <StackLayout>
        <Frame BackgroundColor="White" CornerRadius="6" Padding="8" HasShadow="False">
            <SearchBar x:Name="searchArtist" Placeholder="Search Artists" TextColor="Black" HeightRequest="48"/>
        </Frame>
        <ActivityIndicator IsVisible="{Binding IsLoading}" IsRunning="{Binding IsLoading}" Margin="0, 20"/>
        <ListView x:Name="ArtistList"
                  ItemsSource="{Binding Artists}"
                  SelectedItem="{Binding SelectedArtist}"
                  CachingStrategy="RecycleElement"
                  SeparatorVisibility="None"
                  Margin="8, 4"
                  RowHeight="80">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <templates:ItemList />
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>